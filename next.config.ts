import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      'qkyaigeccoxcigkweqmu.supabase.co', // Agrega tu dominio de Supabase aquí
    ],
  },
};

export default nextConfig;
